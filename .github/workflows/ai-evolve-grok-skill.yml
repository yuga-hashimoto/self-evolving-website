name: AI Evolution (Grok) - Skill Version

on:
  # schedule:
  #   # 既存のGrokとずらして実行（30分後）
  #   - cron: '0 10 * * *'   # UTC 10:00 (JST 19:00)
  #   - cron: '0 22 * * *'   # UTC 22:00 (JST 翌7:00)
  workflow_dispatch:

jobs:
  evolve-grok-skill:
    uses: ./.github/workflows/reusable-evolve-model-skill.yml
    with:
      model_id: 'grok'
      model_display_name: 'Grok'
      anthropic_model: 'x-ai/grok-code-fast-1'
      commit_prefix: '🚀 AI Evolution (Skill)'
    secrets:
      model_api_key: ${{ secrets.GROK_API_KEY }}
      ga4_property_id: ${{ secrets.GA4_PROPERTY_ID }}
      google_application_credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
      serper_api_key: ${{ secrets.SERPER_API_KEY }}
