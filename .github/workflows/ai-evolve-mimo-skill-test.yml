name: AI Evolution (MiMo) - Test Version

on:
  workflow_dispatch:  # 手動実行のみ

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  evolve-mimo-skill:
    uses: ./.github/workflows/reusable-evolve-model-skill-test.yml
    with:
      model_id: 'mimo'
      model_display_name: 'MiMo (Test)'
      anthropic_model: 'xiaomi/mimo-v2-flash:free'
      commit_prefix: '🧪 AI Evolution (Test)'
    secrets:
      model_api_key: ${{ secrets.MIMO_API_KEY }}
      ga4_property_id: ${{ secrets.GA4_PROPERTY_ID }}
      google_application_credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
      serper_api_key: ${{ secrets.SERPER_API_KEY }}
