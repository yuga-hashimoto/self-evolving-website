name: AI Evolution (MiMo) - Skill Version

on:
  # schedule:
  #   # 既存のMiMoとずらして実行（30分後）
  #   - cron: '30 9 * * *'   # UTC 9:30 (JST 18:30)
  #   - cron: '30 21 * * *'  # UTC 21:30 (JST 翌6:30)
  workflow_dispatch:

jobs:
  evolve-mimo-skill:
    uses: ./.github/workflows/reusable-evolve-model-skill.yml
    with:
      model_id: 'mimo'
      model_display_name: 'MiMo'
      anthropic_model: 'xiaomi/mimo-v2-flash:free'
      commit_prefix: '🆓 AI Evolution (Skill)'
    secrets:
      model_api_key: ${{ secrets.MIMO_API_KEY }}
      ga4_property_id: ${{ secrets.GA4_PROPERTY_ID }}
      google_application_credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
      serper_api_key: ${{ secrets.SERPER_API_KEY }}
