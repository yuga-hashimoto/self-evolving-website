name: AI Evolution - Free (MiMo)

on:
  schedule:
    - cron: '0 9 * * *'   # 毎日UTC 9:00 (JST 18:00)
    - cron: '0 21 * * *'  # 毎日UTC 21:00 (JST 6:00)
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  evolve-mimo:
    uses: ./.github/workflows/reusable-evolve-model.yml
    with:
      model_id: 'mimo'
      model_display_name: 'MiMo'
      anthropic_model: 'xiaomi/mimo-v2-flash:free'
      commit_prefix: '🆓 AI Evolution'
    secrets:
      model_api_key: ${{ secrets.MIMO_API_KEY }}
      ga4_property_id: ${{ secrets.GA4_PROPERTY_ID }}
      google_application_credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
